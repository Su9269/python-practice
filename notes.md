# Python 學習筆記

## Pandas
- groupby → 分組計算
- apply → 把函式套用到每一行
- isin → 篩選某欄位是否在清單裡
- value_counts → 算每個值出現幾次

## Matplotlib
- barh → 水平長條圖
- bar → 垂直長條圖
- scatter → 散佈圖
- annotate → 在圖上標文字
- tight_layout → 自動調整間距避免文字被切掉